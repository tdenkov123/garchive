{
  "annotations": {"list": []},
  "title": "GArchive gRPC",
  "panels": [
    {
      "title": "Request rate",
      "targets": [{"expr": "sum(rate(grpc_requests_total[5m])) by (method)"}]
    },
    {
      "title": "Latency p99",
      "targets": [{"expr": "histogram_quantile(0.99, sum(rate(grpc_request_duration_seconds_bucket[5m])) by (le, method))"}]
    }
  ],
  "schemaVersion": 39,
  "version": 1
}
